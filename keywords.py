#the parse pdf function will return the file_path(file name) along with metadata
# at the index 1 and the data at the index 2 ( i.e it will return a list consisting 3 data)

from parsing import parse_pdf




def list_file_
print(data)